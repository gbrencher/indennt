#!/usr/bin/env python

from distutils.core import setup
import setuptools

setup(name='indennt',
      version='0.1',
      description='Interferogram denoising convolutional neural network',
      packages=setuptools.find_packages(),
     )
